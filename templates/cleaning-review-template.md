# 清洗识别结果

## 识别结果

- 源文件：
- 源工作表：
- 说明区：
- 字段表头行：
- 跳过的系统 / 英文字段行：
- 数据区：
- 排除行：
- 预计输出明细行数：
- 置信度：

## 同名 Tab 合并检查

### Tab「{sheet_name}」

**表头检查：** `{一致 / 不一致}`

| 来源文件 | 实际数据行数 | 排除行数 | 表头检查 |
| --- | ---: | ---: | --- |
| `{absolute_source_path_1}` | {actual_rows_1} | {skipped_rows_1} | 一致 |
| `{absolute_source_path_2}` | {actual_rows_2} | {skipped_rows_2} | 一致 |

**合并汇总：**

- 来源文件数：{source_count}
- 各来源实际数据合计：{merged_data_rows}
- 合并后预计数据行数：{merged_data_rows}
- 输出 Tab 名：`{sheet_name}`

如果表头不一致，必须列出具体差异：

| 列序号 | 参考表头 | 当前表头 | 来源文件 |
| ---: | --- | --- | --- |
| {column_index} | `{reference_header}` | `{candidate_header}` | `{absolute_source_path}` |

## 识别依据

-

---

### 需要你确认

> **问题：** 是否按上述规则清洗，并对表头一致的同名 Tab 执行合并？

**可选操作**

- `1｜确认清洗并合并（推荐）`：同名且表头一致的 Tab 合并到一个同名 Tab。
- `2｜确认清洗但分别保留`：同名 Tab 按来源文件分别输出。
- `3｜调整规则`：修改表头、数据区、字段映射或排除行。
- `4｜暂停处理`：补充文件或业务说明后再继续。

**请回复：** `1`、`2`、`3` 或 `4`

## 输出约束

- 原文件：只读，不修改
- 输出文件：必须显示绝对路径
- 同名 Tab 合并时：使用原 Tab 名，不添加月份后缀
- 追溯字段：`源文件名`、`源工作表名`、`源行号`
