# 清洗识别结果

## 当前目标

- 目标 ID：
- 已确认目标：
- 契约指纹：
- 独立输出文件：

## 目标数据范围

- 纳入文件 / Sheet：
- 排除范围：
- 必需字段：
- 辅助字段：
- 关联键 / 时间字段：

## 字段映射

| 来源文件 / Sheet | 来源字段 | 标准字段 | 映射置信度 | 状态 |
| --- | --- | --- | --- | --- |

## 识别结果

- 源文件：
- 源工作表：
- 说明区：
- 字段表头行：
- 跳过的系统 / 英文字段行：
- 数据区：
- 排除候选行：
- 待确认候选数：
- 当前保留明细行数（候选尚未确认时按保留计）：
- 置信度：

## 排除候选确认

| 候选 ID | 来源行 | 类型 | 命中内容 | 判定依据 | 建议 | 当前决策 |
| --- | ---: | --- | --- | --- | --- | --- |
| `{candidate_id}` | {source_row} | `{summary/note/signoff/sparse}` | `{matched_content}` | {evidence} | 排除 | `pending` |

候选行只代表需要确认，不会在 dry-run 阶段自动删除。正式执行前，每条候选都必须明确标记为 `exclude` 或 `keep`。

## 同名 Tab 合并检查

### Tab「{sheet_name}」

**表头检查：** `{一致 / 不一致}`

| 来源文件 | 实际数据行数 | 排除行数 | 表头检查 |
| --- | ---: | ---: | --- |
| `{absolute_source_path_1}` | {actual_rows_1} | {skipped_rows_1} | 一致 |
| `{absolute_source_path_2}` | {actual_rows_2} | {skipped_rows_2} | 一致 |

**合并汇总：**

- 来源文件数：{source_count}
- 各来源实际数据合计：{merged_data_rows}
- 合并后预计数据行数：{merged_data_rows}
- 输出 Tab 名：`{sheet_name}`

如果表头不一致，必须列出具体差异：

| 列序号 | 参考表头 | 当前表头 | 来源文件 |
| ---: | --- | --- | --- |
| {column_index} | `{reference_header}` | `{candidate_header}` | `{absolute_source_path}` |

## 识别依据

-

---

### 需要你确认

> **问题：** 是否按已确认目标、字段映射、质量处置和排除建议生成独立定向清洗文件？

**可选操作**

- `1｜接受建议并清洗（推荐）`：按建议排除候选行，并按已确认的合并方式执行。
- `2｜逐项调整候选`：指定哪些候选保留、哪些候选排除。
- `3｜调整识别规则`：修改表头、数据区、字段映射或候选识别规则后重新 dry-run。
- `4｜暂停处理`：补充文件或业务说明后再继续。
- `5｜返回目标首页`：保留当前目标和 dry-run，稍后继续。

**请回复：** `1`、`2`、`3`、`4` 或 `5`

## 输出约束

- 原文件：只读，不修改
- 输出文件：必须显示绝对路径
- 同名 Tab 合并时：使用原 Tab 名，不添加月份后缀
- 追溯字段：`源文件名`、`源工作表名`、`源行号`
- 清洗输出：新增 `数据说明` 和 `清洗审计` Tab
